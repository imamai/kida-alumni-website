/**
 * Placeholder until generated from the live schema:
 *   npx supabase gen types typescript --project-id sedsjjmjnikppfaecaya > src/lib/supabase/types.ts
 * Run that after each migration lands so query results stay typed.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
